# src/pi

This is the one place in jpi allowed to reach past Pi's public extension API.
Everything here either mirrors a Pi-internal type, monkeypatches a Pi object,
or duplicates behavior Pi keeps unexported. When Pi ships a new version, this
directory is where to check.

## Rules

- `src/pi/` is the only non-test directory allowed to declare Pi-internal
  type mirrors or patch/reach into Pi objects.
- `src/core` may import from `src/pi`. `src/pi` must never import from
  `src/core` — the dependency only runs one way.
- A coupling that can't be moved here yet (it's still entangled with a
  module) gets a marker comment at its call site instead:

  ```
  // pi-internal(<topic>): <one-line why>
  ```

  `<topic>` groups related markers so they can be swept together later. See
  "Markers elsewhere" below for the sites that carry one today.

## Couplings in this directory

| File                                                                                                                                                                                    | Depends on (upstream)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Verified against                                                               | Fails if upstream changes                                                                                                                                                                                                                                                                   | Canary |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| `types.ts`                                                                                                                                                                              | The shape of Pi's `pi.events` bus (`emit`/`on`) and the `onBeforeAgentStart` event payload (`{ systemPrompt }`) — neither is an exported type from `@earendil-works/pi-coding-agent`, so these are hand-written mirrors. Also mirrors `ToolRenderContext` (`renderCall`/`renderResult`'s fourth argument) and `TranscriptEntryLike` (pi's own `SessionEntry`, covering both history's narrow read and guardian's wider one — `toolName`/`details` included), neither of which pi-coding-agent's root barrel re-exports. | `@earendil-works/pi-coding-agent@0.84.4`                                       | Pi renames or reshapes the bus methods, changes the before-agent-start payload, the tool-render context shape, or the session-entry shape (`type`/`message.role`/`message.content`/`message.toolName`/`message.details`) — jpi's mirrors silently drift out of sync with no compiler error. | TBD    |
| `types.ts` (`ToolResultEventResult`)                                                                                                                                                    | The root barrel exports `ToolCallEventResult` but omits this sibling type — a gap in pi-coding-agent 0.84.3 — so guardian's `handleToolResult` mirrors it by hand.                                                                                                                                                                                                                                                                                                                                                      | `@earendil-works/pi-coding-agent@0.84.3`                                       | pi-coding-agent starts exporting `ToolResultEventResult` (this mirror becomes redundant, not wrong) or reshapes the real type — jpi's mirror silently drifts out of sync with no compiler error.                                                                                            | TBD    |
| `types.ts` (`DialogUIContext`, `SessionManagerLike`, `ModelRegistryLike`)                                                                                                               | Structural subsets of pi's real `ExtensionUIContext` (`select`/`input` dialog methods), `SessionManager` (`getBranch`), and `ModelRegistry` (`find`/`hasConfiguredAuth`/`complete`) — guardian only needs a slice of each and a full import would pull in more than its test doubles honestly implement.                                                                                                                                                                                                                | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi renames or reshapes any of these members — guardian's `ReviewContext` would silently stop matching what pi actually hands its `tool_call` handler, surfacing as a runtime `undefined` call rather than a compiler error.                                                                 | TBD    |
| `types.ts` (`MessageContentPart`, `MessageUsageLike`, `MessageLike`, `BranchEntryLike`, `SessionStartInput`, `SessionInfoChangedInput`, `MessageEventInput`, `ToolExecutionStartInput`) | jpi-status's own local re-declaration of pi's message/usage/session-entry/event shapes, read defensively (`?? 0`) since pi's real `AgentMessage`/`SessionEntry`/lifecycle-event types don't narrow cleanly in every footer-stats context that reads them.                                                                                                                                                                                                                                                               | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi renames or reshapes any of these fields (`usage.input`/`cost.total`, `message.role`/`stopReason`, event `name`/`toolName`) — jpi-status's footer stats would silently stop accumulating or reset to zero rather than erroring.                                                           | TBD    |
| `types.ts` (`FooterData`, `FooterContext`)                                                                                                                                              | Structural mirrors of pi's `ReadonlyFooterDataProvider` (the object a `setFooter` factory receives at render time) and the footer's own `ExtensionContext` — neither is exported from pi-coding-agent's root barrel.                                                                                                                                                                                                                                                                                                    | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi renames or reshapes `setFooter`'s factory signature, `getExtensionStatuses`/`onBranchChange`, or the footer's `ExtensionContext` fields — jpi-status's footer would silently stop compiling against what pi actually calls it with.                                                      | TBD    |
| `settings.ts` (`getAgentDirectory`)                                                                                                                                                     | Pi's own agent-directory resolution: `PI_CODING_AGENT_DIR` env var, default `~/.pi/agent`. Pi doesn't export this logic, so jpi re-derives it to find `jpi.kdl` and friends.                                                                                                                                                                                                                                                                                                                                            | `@earendil-works/pi-coding-agent@0.84.4`                                       | Pi changes the env var name or the default path — jpi and Pi resolve to different directories and jpi's config goes silently missing (or gets created somewhere new).                                                                                                                       | TBD    |
| `extension-api.ts` (`cloneExtensionApi`)                                                                                                                                                | `ExtensionAPI`'s members being own enumerable properties, so `{ ...pi, ...overrides }` picks up every one of them.                                                                                                                                                                                                                                                                                                                                                                                                      | `@earendil-works/pi-coding-agent@0.84.4`                                       | Pi turns `ExtensionAPI` into a class instance, or moves members onto a prototype or behind getters — the spread stops copying them and every decorated method silently disappears.                                                                                                          | TBD    |
| `markdown.ts` (`resolveMarkdownTheme`)                                                                                                                                                  | `getMarkdownTheme()`'s lazy-throw behavior: it returns arrow functions that read Pi's global theme lazily, so calling one before `initTheme()` has run throws inside `render()`, not at the `getMarkdownTheme()` call site.                                                                                                                                                                                                                                                                                             | `@earendil-works/pi-coding-agent@0.84.4`                                       | Pi changes `getMarkdownTheme()` to throw eagerly (breaking the probe-based try/catch) or to return successfully with an unusable theme (silently skipping the fallback when it shouldn't).                                                                                                  | TBD    |
| `markdown.ts` (`disableThinkingItalics`)                                                                                                                                                | Monkeypatches pi-tui's `Markdown.prototype.applyDefaultStyle`/`getDefaultStylePrefix` to strip the italic pi hardcodes onto assistant thinking blocks. Depends on those method names and on `defaultTextStyle` staying the instance field pi-tui reads for the default style (a real ES class field, so it can't be intercepted with a prototype accessor).                                                                                                                                                             | `@earendil-works/pi-tui` bundled with `@earendil-works/pi-coding-agent@0.84.4` | pi-tui renames `applyDefaultStyle`/`getDefaultStylePrefix`, renames `defaultTextStyle`, or stops giving thinking-block `Markdown` instances `defaultTextStyle.italic: true` — the patch silently degrades to a no-op (italics stay) rather than crashing.                                   | TBD    |
| `user-message.ts` (`removeUserMessagePadding`)                                                                                                                                          | Monkeypatches pi-coding-agent's `UserMessageComponent.prototype.rebuild` to zero the vertical padding (`paddingY`) of the pi-tui `Box` it builds for the background fill. Depends on `rebuild()` adding exactly one content child shaped like a pi-tui `Box` (a plain `paddingY` field plus `invalidateCache()`).                                                                                                                                                                                                       | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi changes how `UserMessageComponent.rebuild()` builds its padding child, or renames `rebuild`/`paddingY`/`invalidateCache` — the patch silently degrades to a no-op (padding stays).                                                                                                       | TBD    |
| `extension-runner.ts` (`patchToolDefinitionLookup`)                                                                                                                                     | Monkeypatches `ExtensionRunner.prototype.getToolDefinition`, the chokepoint pi's own execution machinery and the HTML transcript exporter use to look up a tool definition by name. Depends on that method's name and pi's root barrel continuing to export `ExtensionRunner`.                                                                                                                                                                                                                                          | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi renames `getToolDefinition`, changes its per-extension lookup shape, or stops exporting `ExtensionRunner` — the patch silently degrades to a no-op (callers get pi's original, unreskinned lookup).                                                                                      | TBD    |
| `messages.ts` (`editResultDiff`, `countDiffStats`)                                                                                                                                      | `EditToolDetails.diff`'s shape (an `unknown`-typed field on the wire) and pi's own diff string format (`generateDiffString`'s `+`/`-`/`+++`/`---` line prefixes), used to summarize an edit result as an addition/removal count.                                                                                                                                                                                                                                                                                        | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi changes `EditToolDetails`'s field name/shape, or changes its diff string format (e.g. adding a new line-prefix convention) — stats silently come out wrong (not a crash) since both functions just pattern-match strings/shapes.                                                         | TBD    |
| `editor.ts` (`MinimalLayoutBox`/`MinimalLayoutFrame`, `detectEditorAccess`, `patchViewportInput`)                                                                                       | pi-tui's unexported layout-box/layout-frame shape, `Editor`'s private `state`/`scrollOffset`/`buildVisualLineMap`/`pushUndoSnapshot`, and `TUI`'s private `handleViewportInput`/`currentLayout` — none of these are part of pi-tui's public `Editor`/`TUI` API, so jpi reaches past it for click-to-move-cursor and drag-to-select.                                                                                                                                                                                     | `@earendil-works/pi-tui` bundled with `@earendil-works/pi-coding-agent@0.84.4` | pi-tui renames or reshapes any of these private fields/methods, or changes the layout box shape — `detectEditorAccess`/`patchViewportInput` degrade to a no-op (mouse support silently doesn't install) rather than crashing.                                                               | TBD    |
| `editor.ts` (`computePaddingX`, `computeLayoutWidth`, `computeMaxVisibleLines`)                                                                                                         | Hand-mirrors of `Editor#render`'s own internal padding-clamp, layout-width, and max-visible-lines formulas (not exposed by pi-tui), needed to convert a screen click into a logical text position the same way `Editor#render` itself would.                                                                                                                                                                                                                                                                            | `@earendil-works/pi-tui` bundled with `@earendil-works/pi-coding-agent@0.84.4` | pi-tui changes any of these formulas in `Editor#render` — jpi's mirrors silently drift, misplacing clicks/selection by a column or two rather than erroring.                                                                                                                                | TBD    |
| `editor.ts` (`matchIndices`)                                                                                                                                                            | Mirrors pi-tui's `fuzzyMatch` loop (minus its letter/digit-swap fallback) so the prompt-history picker can highlight the same characters `fuzzyFilter` matched on, since pi-tui exposes the filter but not the per-character match indices.                                                                                                                                                                                                                                                                             | `@earendil-works/pi-tui` bundled with `@earendil-works/pi-coding-agent@0.84.4` | pi-tui changes `fuzzyMatch`'s matching algorithm — jpi's highlighted characters stop lining up with which entries `fuzzyFilter` actually ranked highest, a cosmetic drift, not a crash.                                                                                                     | TBD    |
| `editor.ts` (`computeOverlayMaxHeightRows`)                                                                                                                                             | Mirrors pi-tui's own `SizeValue` percentage parsing (`parseSizeValue`'s unexported `"N%"` branch, `Math.floor((referenceSize * pct) / 100)`) — used by `/btw`'s overlay to size its own viewport to match the `maxHeight: "N%"` it passes to `ctx.ui.custom()`.                                                                                                                                                                                                                                                         | `@earendil-works/pi-tui` bundled with `@earendil-works/pi-coding-agent@0.84.4` | pi-tui changes its percentage-to-rows rounding (e.g. `Math.round` instead of `Math.floor`) or its clamping — `/btw`'s viewport estimate silently drifts from the overlay's actual rendered height by a row, clipping or padding the last line.                                              | TBD    |
| `extension-runner.ts` (`listExtensionTools`)                                                                                                                                            | Reads `loader.getExtensions().extensions` and each `Extension`'s own `tools` Map directly — the same maps `registerTool` writes into, so late-registering extensions (pi-mcp from `session_start`, context-mode from `before_agent_start`) are only visible on a fresh call, never a cached one.                                                                                                                                                                                                                        | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi renames/reshapes `Extension.tools`, or stops writing into it from `registerTool` — subagent tool scoping would silently stop seeing newly-registered tools.                                                                                                                              | TBD    |
| `extension-runner.ts` (`emitSessionShutdown`)                                                                                                                                           | Manually replicates pi's own (unexported) `emitSessionShutdownEvent` helper — checks `ExtensionRunner.hasHandlers("session_shutdown")` then calls `.emit()` — because pi's root barrel doesn't export that helper. Depends on `AgentSession.dispose()` continuing to skip this emit itself (see the long #242 comment in the function).                                                                                                                                                                                 | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi starts emitting `session_shutdown` from `dispose()` itself (this becomes a harmless double-emit) or renames `hasHandlers`/`emit` (this silently degrades to a no-op, re-introducing #242's leak).                                                                                        | TBD    |
| `agent.ts` (`wrapBeforeToolCall`)                                                                                                                                                       | Reassigns `Agent.beforeToolCall` after construction rather than through a documented multi-hook API (pi's `Agent` has none) — pi's execution loop reads whatever this field holds at call time, and this has to chain to whatever hook was already installed.                                                                                                                                                                                                                                                           | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi renames `beforeToolCall`, changes its context/result shape, or starts reading a snapshot of the hook taken before extensions run — the chained wrapper would stop firing or silently drop the prior hook.                                                                                | TBD    |
| `agent.ts` (`setSystemPrompt`, `pushMessages`)                                                                                                                                          | Writes `Agent.state.systemPrompt`/`state.messages` directly. `state.messages` is an accessor pair where assigning a new array copies it — `pushMessages` mutates in place via the getter so the session keeps using the same array identity.                                                                                                                                                                                                                                                                            | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi makes `state.systemPrompt` read-only, or changes `state.messages` so the getter no longer returns a live, mutable reference — a push would silently stop reaching the session's real transcript.                                                                                         | TBD    |
| `model-registry.ts` (`getModelRuntime`)                                                                                                                                                 | Reads the private `runtime` field off `ExtensionContext.modelRegistry` — pi 0.80.8 moved `createAgentSession`'s model option from `modelRegistry` to `modelRuntime`, but `ExtensionContext` still only exposes the registry facade, not the runtime.                                                                                                                                                                                                                                                                    | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi renames/removes the private `runtime` field, or stops accepting `modelRegistry` on `createAgentSession` for the pre-0.80.8 shim — a caller building its own session would silently lose the parent's model providers.                                                                    | TBD    |
| `settings.ts` (`resolveDefaultSessionDir`)                                                                                                                                              | Mirrors pi's own session-directory precedence: `PI_CODING_AGENT_SESSION_DIR` env var, then `SettingsManager.getSessionDir()`, which pi doesn't expose a combined resolver for.                                                                                                                                                                                                                                                                                                                                          | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi changes the env var name, or `SettingsManager` stops carrying `getSessionDir()` — jpi's spawned sessions would land in the wrong directory with no error.                                                                                                                                | TBD    |
| `settings.ts` (`settingsFilePaths`, `readSettingsField`)                                                                                                                                | Reads `<cwd>/.pi/settings.json` and `<agentDir>/settings.json` directly and mirrors pi's own `SettingsManager` project-over-global deep-merge for one field at a time, since `SettingsManager` exposes no generic per-field getter.                                                                                                                                                                                                                                                                                     | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi changes the settings file paths, changes the merge precedence (project no longer wins), or changes the file format — callers (e.g. subagents' `enabledModels`) would silently read stale or wrong values.                                                                                | TBD    |
| `settings.ts` (`workspaceAgentsDir`, `personalAgentsDir`, `findAgentFile`)                                                                                                              | Mirrors pi's own custom-agent-file discovery precedence (`.agents/agents/` then `<agentDir>/agents/`) — pi exposes no standalone "find this agent's file" lookup.                                                                                                                                                                                                                                                                                                                                                       | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi changes the discovery roots or their precedence — `/agents` file-editing operations would silently target the wrong file, or none.                                                                                                                                                       | TBD    |
| `skills.ts` (`skillDiscoveryRoots`, `findSkillInRoot`)                                                                                                                                  | Mirrors pi's own skill-discovery layout: the 4 roots in precedence order, the `<name>/SKILL.md` layout rule, and "skills don't nest" — pi exposes no standalone lookup for jpi's skill preloading to call. Deliberately DEVIATES from pi by rejecting symlinks (roots, flat `.md` files, `SKILL.md` files); pi itself follows them.                                                                                                                                                                                     | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi changes a discovery root, the layout rule, or the nesting rule — preloaded skill content would silently come from the wrong file or go missing. The symlink rejection is an intentional, permanent divergence, not something to keep in sync.                                            | TBD    |
| `session-stats.ts` (`getSessionTokens`)                                                                                                                                                 | Reads `AgentSession.getSessionStats().tokens`, deliberately excluding pi's own `tokens.total` field, which sums per-turn `cacheRead` and so double-counts a repeatedly re-read cached prefix across turns (issue #38).                                                                                                                                                                                                                                                                                                  | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi changes what `tokens.total` means (e.g. stops double-counting), making this workaround unnecessary but harmless, or changes `getSessionStats()`'s shape/reset-at-compaction behavior — the token count would silently drift from what pi itself reports.                                 | TBD    |
| `types.ts` (`THINKING_LEVELS`)                                                                                                                                                          | Hand-mirrors pi-ai's `EXTENDED_THINKING_LEVELS` ordering (`off` + every `ThinkingLevel`) — pi-ai exports the `ThinkingLevel` union type but not the ordering as a value (drift risk flagged as #147).                                                                                                                                                                                                                                                                                                                   | `@earendil-works/pi-ai` bundled with `@earendil-works/pi-coding-agent@0.84.4`  | pi-ai adds/removes/reorders a thinking level — the Agent tool description, the generated-agent template, and the `/agents` wizard would silently offer a stale list.                                                                                                                        | TBD    |
| `types.ts` (`WidgetTheme`, `WidgetUIContext`, `FleetUIContext`, `FocusAwareTui`)                                                                                                        | Structural mirrors of narrow slices of pi's real `ExtensionUIContext`/`Theme`/`TUI` — `setWidget`/`setStatus`/`onTerminalInput`/`getEditorText`/`custom`/`getFocusedComponent` — used where jpi widgets only need part of the real surface and a full import would pull in more than the call site can honestly type-check against.                                                                                                                                                                                     | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi renames or reshapes any of these `ctx.ui`/`TUI` members — the `as`/`as unknown as` casts at the boundary (subagents' `index.ts`) would silently paper over a mismatch instead of erroring.                                                                                               | TBD    |
| `messages.ts` (`messageUsage`, `toolCallName`, `asBashExecution`)                                                                                                                       | Reads `AgentMessage`/content-block fields (`usage`, tool-call `name`/`toolName`, bash-execution `role`/`command`/`output`) through an `any` escape hatch — pi's `AgentMessage` union (`Message \| CustomAgentMessages[...]`) doesn't narrow cleanly to the specific variant in every context these are read from.                                                                                                                                                                                                       | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi renames these fields, or changes which variant carries them — callers' `?? 0`/`?? "unknown"` defensive reads would silently fall back instead of surfacing real data.                                                                                                                    | TBD    |
| `markdown.ts` (`resolveWidgetMarkdownTheme`)                                                                                                                                            | Same probe/fallback shape as `resolveMarkdownTheme` above, but for callers that only carry jpi's narrow `WidgetTheme` (`{ fg, bold }`) instead of pi's real `Theme` — kept as a separate function since the two fallback themes derive their styles differently.                                                                                                                                                                                                                                                        | `@earendil-works/pi-coding-agent@0.84.4`                                       | Same as `resolveMarkdownTheme` above: pi changes `getMarkdownTheme()`'s throw timing.                                                                                                                                                                                                       | TBD    |
| `system-prompt.ts` (`appendPiTail`)                                                                                                                                                     | Reproduces the `customPrompt` branch of pi's own unexported `buildSystemPrompt` (`dist/core/system-prompt.js`) byte-for-byte: appended system-prompt text, the `<project_context>` block, the skills section, and the trailing "Current working directory" line, in that order.                                                                                                                                                                                                                                         | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi reorders or changes any of those sections in `buildSystemPrompt` — jpi's custom system prompt would silently drift from what stock pi produces for the same options.                                                                                                                     | TBD    |
| `system-prompt.ts` (`buildToolList`, `buildGuidelines`, `buildPiDocsBlock`, `buildEnvironment`)                                                                                         | Mirrors stock pi's own system-prompt sections: the "Available tools" list built from `selectedTools`/`toolSnippets`, the prompt-guidelines bullets, the "Pi documentation" block (doc filenames included), and the environment-facts block format.                                                                                                                                                                                                                                                                      | `@earendil-works/pi-coding-agent@0.84.4`                                       | pi changes any of these sections' wording, field set, or format — jpi's custom system prompt template would render stale or mismatched text with no compiler error.                                                                                                                         | TBD    |

## Markers elsewhere

| Topic                             | Where                                                                                                 | Why it stays a marker instead of a move                                                                                                                                                                                                                                                                                                                                                                               |
| --------------------------------- | ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `builtin-tool-shadowing`          | `modules/style/index.ts` (top of `registerStyleTools`)                                                | Relies on pi's undocumented registration-order shadowing of built-ins: extensions that call `pi.registerTool` with a built-in's own name (`read`, `bash`, `edit`, ...) shadow pi's version. This is jpi's own tool-registration code, not a Pi-internal reach-in, so it stays in the module — but its correctness depends on `extensions/jpi/index.ts`'s module load order, which is load-bearing and worth flagging. |
| `system-prompt-replace`           | `modules/prompt/extension.ts` (`onBeforeAgentStart`)                                                  | Returning `{ systemPrompt }` replaces pi's whole system prompt outright, rather than appending to it. This is jpi's own extension logic, not a Pi reach-in by itself, but its correctness depends on pi's undocumented "last extension to set the prompt wins" rule and on `extensions/jpi/index.ts` loading `promptModule` before `memoryModule`/`scratchpadModule`.                                                 |
| `private-editor-history`          | `modules/history/editor.ts` (`HistoryEditor`'s `submissions` field)                                   | `CustomEditor`'s own submission history is private, so `HistoryEditor` keeps its own duplicate list to seed from the prompt log and replay anything submitted before that read finishes. Drifts if pi's private history field changes what it tracks or when.                                                                                                                                                         |
| `shortcut-conflict-dodge`         | `modules/history/editor.ts` (`HistoryEditor.handleInput`, ctrl+r)                                     | ctrl+r opens the prompt picker directly in `handleInput` instead of via `pi.registerShortcut`, which flags a startup conflict for any shortcut that shadows a built-in default (ctrl+r is `app.session.rename`'s). Depends on pi continuing to route unregistered input through the editor's `handleInput`.                                                                                                           |
| `editor-slot-last-wins`           | `modules/history/index.ts` (`sessionCtx.ui.setEditorComponent` call in `registerHistory`)             | `setEditorComponent` has exactly one slot; whichever extension calls it last during `session_start` wins. jpi relies on `extensions/jpi/index.ts` loading `historyModule` last so its editor is the one that sticks.                                                                                                                                                                                                  |
| `turn-end-ordering`               | `modules/subagents/agent-runner.ts` (`installExtensionToolScope`)                                     | The active-set renarrow on `turn_end` depends on pi emitting `turn_end` synchronously before `prepareNextTurn` re-snapshots `agent.state.tools`. This is jpi's own scoping logic, not a Pi reach-in by itself, but its correctness rests on that undocumented event ordering.                                                                                                                                         |
| `prompt-append-order`             | `modules/subagents/agent-runner.ts` (`runAgent`, AGENTS.md/CLAUDE.md suppression)                     | Depends on pi's `buildSystemPrompt()` re-appending AGENTS.md/CLAUDE.md/APPEND_SYSTEM.md AFTER `systemPromptOverride` — that ordering is what `prompt_mode: replace`/`isolated: true` rely on to suppress them by omission rather than an explicit flag.                                                                                                                                                               |
| `compaction-trim-timing`          | `modules/subagents/output-file.ts` (`streamToOutputFile`, `queueMicrotask` re-anchor)                 | The re-anchor after `compaction_end` is deferred a microtask because pi's overflow-retry path trims the trailing error assistant message AFTER emitting `compaction_end` (#145) — anchoring synchronously would sit one past the trimmed array.                                                                                                                                                                       |
| `compaction-resets-session-stats` | `modules/subagents/usage.ts` (`LifetimeUsage`)                                                        | `LifetimeUsage` exists only because pi resets session-derived stats at compaction (replaces `session.state.messages`, which `getSessionStats()` derives from — see `src/pi/session-stats.ts`'s `getSessionTokens`) — an undocumented pi behavior this independent accumulator works around.                                                                                                                           |
| `append-only-providers`           | `modules/subagents/index.ts` (agent-mention toggle comment; autocomplete provider registration guard) | Both depend on pi's autocomplete wrapper list being append-only (never pruned) — the toggle relies on the provider staying registered forever and just delegating when off; the registration guard relies on a second call layering a duplicate rather than replacing.                                                                                                                                                |
| `factory-vs-filter-order`         | `modules/subagents/index.ts` (RPC handler wiring, session_start gating, #142)                         | RPC handlers are wired on `session_start` rather than at factory time because pi runs every extension factory before applying the `extensions:` filter and only fires lifecycle events for survivors — a filtered-out child session must not advertise or answer RPC.                                                                                                                                                 |
| `tool-result-usage-folding`       | `modules/subagents/index.ts` (`withUsageReporting`)                                                   | Depends on pi copying `AgentToolResult.usage` onto the persisted tool-result message and folding it into `getSessionStats()` — the only route jpi has found back into the parent session's footer/statusline/`/cost` for a subagent's spend.                                                                                                                                                                          |
| `notools-empty-allowlist`         | `modules/subagents/mention-clone.ts` (clone session's `tools:` allowlist)                             | `noTools: "all"`'s doc comment reads like it spares custom tools; it actually resolves to an empty allowlist and `isAllowedTool` drops every tool, custom ones included. Works around this by passing an explicit allowlist naming exactly the clone's own tool instead.                                                                                                                                              |
| `throw-only-tool-failure`         | `modules/subagents/agent-manager.ts` (`spawnAndWait`, startup-failure rethrow)                        | The immediate/blocking spawn path rethrows a startup failure to its caller because pi only marks a tool result failed when `execute` throws — a resolved result with an error field wouldn't register as a failure upstream.                                                                                                                                                                                          |
| `pi-tui-single-copy`              | `modules/subagents/ui/fleet-list.ts` (`editorHasFocus`, `instanceof Editor`)                          | The identity check relies on the loader aliasing pi-tui to pi's own single copy, so every `Editor` instance in the process shares one prototype. A second, unaliased copy of pi-tui in the module graph would break `instanceof`.                                                                                                                                                                                     |
| `input-dispatch-order`            | `modules/subagents/ui/fleet-list.ts` (`handleKey`, #123)                                              | The focus check depends on global input listeners (`onTerminalInput`) firing BEFORE the focused component, so a dialog that swapped the prompt editor out is caught before `getEditorText()` reads the detached, empty editor.                                                                                                                                                                                        |
| `message-identity-stability`      | `modules/subagents/ui/conversation-viewer.ts` (`buildEnrichedLines`, block cache)                     | The per-message render cache keys on message object identity, depending on `session.messages` never replacing an already-appended entry in place — only the actively-streaming last message is expected to mutate.                                                                                                                                                                                                    |
| `unhandled-provider-rejection`    | `modules/subagents/ui/agent-mention.ts` (`getSuggestions` wrapper, try/catch)                         | Uses try/catch rather than `.catch()` because a wrapped provider that throws synchronously never returns a promise to attach a `.catch()` to, and pi's own `getSuggestions` caller (`components/editor.js`) awaits with no catch of its own — an uncaught rejection there would take the popup down.                                                                                                                  |
| `batch-denial-termination`        | `modules/guardian/controller.ts` (`denyToolCall`)                                                     | Pi only terminates the turn early when every tool result in the current batch sets `terminate`, so an allowed sibling in a parallel batch delays the stop by one batch. The open circuit still blocks (and terminates) every later call.                                                                                                                                                                              |
| `tool-path-resolution`            | `modules/guardian/allowlist.ts` (`getPathToolTarget`)                                                 | write/edit tool inputs both carry `path`, resolved against the handler's cwd by pi's own tool implementation, so the allowlist resolves it the same way — the allowlist and the actual write target must agree on what a path means.                                                                                                                                                                                  |
| `raw-sgr-status`                  | `modules/guardian/controller.ts` (`coloredStatus`)                                                    | jpi-status passes `setStatus` values through to the terminal unmodified, so guardian's short status carries its own truecolor SGR sequence rather than going through a theme.                                                                                                                                                                                                                                         |
| `context-hook-ephemeral`          | `modules/tasks/index.ts` (`pi.on("context", ...)`)                                                    | The `context` hook fires before each LLM call and its return is a non-persisted copy of the messages array, used only for that one request — not something jpi can rely on being visible in the session's stored transcript.                                                                                                                                                                                          |
| `cached-extension-instance`       | `modules/tasks/index.ts` (`pi.on("session_start", ...)`, `isSwitch`)                                  | new/resume/fork reuse the running extension instance (`getExtensions()` is cached), so session-scoped state must be reset by hand; only startup/reload re-run the factory and start clean.                                                                                                                                                                                                                            |
| `cache-prefix-byte-identity`      | `modules/btw/ask.ts` (`buildBtwContext`)                                                              | Relies on `convertToLlm` + `sessionEntryToContextMessages` composing byte-identically to pi's own turn so `/btw`'s system prompt and transcript prefix can hit the same prompt cache.                                                                                                                                                                                                                                 |
| `compaction-rewrites-branch`      | `modules/btw/index.ts` (`session_before_compact`/`session_compact` handlers)                          | Compaction rewrites the session branch out from under a snapshot taken mid-rewrite, so `/btw` refuses to ask while one is running — an undocumented pi behavior, not jpi's own bookkeeping.                                                                                                                                                                                                                           |
| `collapsed-box-mirror`            | `modules/background/notification-renderer.ts` (module doc comment)                                    | The renderer collapses pi's own undocumented fallback: a custom message type with no `MessageRenderer` shows the full LLM-facing content in a padded box. Cosmetic — if pi changes that fallback, this comment's rationale goes stale, not the renderer's correctness.                                                                                                                                                |
| `pi-ai-dedupe`                    | `vite.config.ts` (`test.server.deps.inline`, `resolve.dedupe`)                                        | npm's nested `pi-ai` duplication (top-level + nested under `pi-coding-agent`) requires inlining + dedupe so print-mode e2e tests share one provider registry; production separately relies on the loader aliasing pi-tui to pi's own copy (see `pi-tui-single-copy` above).                                                                                                                                           |

## Upgrade checklist

On a Pi version bump:

1. Run `vp test tests/pi` (this suite doesn't exist yet — a later step adds
   canary tests here, one per coupling above).
2. Review every `pi-internal(...)` marker site for a topic that still says
   "TBD" under Canary — those have no automated check, so read the upstream
   changelog by hand for that topic.
